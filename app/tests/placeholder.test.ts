import { expect, test } from 'vitest'

// Placing here so that vitest has something to run
// Should be removed once actual tests have been written
test('true is true', () => [
    expect(true).toBe(true)
])
